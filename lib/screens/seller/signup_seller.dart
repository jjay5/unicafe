import 'package:flutter/material.dart';

class SignupSellerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Seller Signup')),
      body: Center(
        child: Text('Seller Signup Form Here'),
        // Implement your seller signup form here
      ),
    );
  }
}
